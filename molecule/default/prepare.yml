---
# No-op: runtime_setup.yml installs apt deps during converge; kept for scenario symmetry.
- name: Prepare
  hosts: localhost
  connection: local
  gather_facts: false
  become: true
  tasks:
    - name: Prepare | no-op (runtime_setup.yml handles apt deps)
      ansible.builtin.debug:
        msg: "prepare: nothing to do; runtime_setup.yml installs apt deps during converge"
