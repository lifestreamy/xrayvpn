---
# No-op: runtime_setup.yml installs apt deps during converge; mirrors default/prepare.yml.
- name: Prepare
  hosts: all
  gather_facts: false
  become: true
  tasks:
    - name: Prepare | no-op (runtime_setup.yml handles apt deps)
      ansible.builtin.debug:
        msg: "prepare: nothing to do; runtime_setup.yml installs apt deps during converge"
