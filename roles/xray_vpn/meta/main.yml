---
galaxy_info:
  role_name: xray_vpn
  namespace: lifestreamy
  author: korelov.dev
  description: VLESS Xray Reality VPN server
  license: AGPL-3.0
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble
    - name: Debian
      versions:
        - bookworm
  galaxy_tags: [vpn, xray, vless, reality]
# `dependencies:` intentionally omitted (roles_path resolution breaks syntax-check);
# install community.general separately — see README / setup script.